# Python 3.11+
# [pdf] tire streamlit-pdf (composant d’aperçu st.pdf) — pas le module « pymupdf »
streamlit[pdf]>=1.55,<2
streamlit-pdf>=1.0,<2
pymupdf>=1.24,<2
pandas>=2.2,<3
openpyxl>=3.1,<4
odfpy>=1.4,<2
# Imaging (tous OS / Python 3.12+). Pour Pillow-SIMD (fork plus rapide, x86), voir requirements-pillow-simd.txt.
pillow>=10
